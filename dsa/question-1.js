// create an object that stores individual letters in an array and has a function for displaying letters as a single word

const displayLettersAsWord = (letters) => {
  return letters.join('');
};

module.exports = {
  displayLettersAsWord,
};
